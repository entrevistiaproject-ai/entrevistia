import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Criar Entrevista",
};

export default function CriarEntrevistaLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
